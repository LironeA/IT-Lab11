

using DataBaseCore;
using DataBaseCore.Models;

class Program
{
    public static DataBase db = new DataBase();

    static void Main()
    {
        //Table table = new Table("Users");
        //table.Fields.Add(new Field("Id", FieldType.Int));
        //table.Fields.Add(new Field("Name", FieldType.String));
        //db.AddTable(table);
    }
}